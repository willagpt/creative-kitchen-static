# Creative Kitchen Static

AI-powered static ad image generator for Willa Ltd, branded as "Big Tasty Productions". Generates marketing images from brand DNA analysis, with AI prompt iteration and a review/rating system. Part of the Marketing suite in Willa HQ.

This repo is also the home of the Video Analysis Engine (Railway worker + Supabase edge function pipeline) and the in-flight Organic Intelligence feature (IG + YouTube organic post monitoring). The broader engineering backlog lives in the Asana project linked at the bottom of this file.

## Tech Stack

- **Framework:** Vite + React
- **Database:** Supabase project `ifrxylvoufncdxyltgqt` (URL: `https://ifrxylvoufncdxyltgqt.supabase.co`)
- **Hosting:** Vercel (personal account: jameseatcheflycos-projects)
- **Video pipeline:** Railway (`creative-kitchen-static-production.up.railway.app`, project `triumphant-dedication`)
- **Fonts:** Plus Jakarta Sans, Syne, Instrument Serif (Google Fonts)
- **Repo:** github.com/willagpt/creative-kitchen-static

## Live URL

https://creative-kitchen-static.vercel.app

## Architecture

Vite + React SPA with dark theme. Analyses brand DNA (colours, style, product info), generates AI image prompts across templates, creates images, and tracks reviews/ratings. Extended to ingest competitor ads from Foreplay, analyse competitor videos through a Railway worker, and (in progress) ingest organic posts from Instagram and YouTube.

### Supabase Tables

- `static_runs` — brand DNA generation runs (brand name, URL, product, DNA as JSONB)
- `static_images` — generated images (74+ images). Fields: template_id, template_name, category, prompt, image_url, version
- `static_reviews` — review ratings by claude or user. Ratings: great, good, needs-work, slop. 46+ reviews
- `static_prompt_versions` — prompt iteration history (32+ versions). Tracks template_id, version, prompt text, image_url, brand_dna_snapshot
- `brand_guidelines` — brand identity data. Fields: packaging_format, packaging_specs, colour_palette, typography, tone_of_voice, photo_descriptions
- `competitor_ads` — ~9,900 rows of enriched competitor ads (Simmer data: Jan 11 – Apr 9, 2026). Key fields: `thumbnail_url`, `snapshot_url`, `page_id`, `page_name`, `creative_title`, `creative_body`, `start_date`, `end_date`, `is_active`, `impressions_lower`, `impressions_upper`, `days_active`, `platforms`. **Apr 2026 columns:** `display_format` (IMAGE/VIDEO/DCO), `video_url`, `card_index`, `parent_ad_id`, `emotional_drivers`, `content_filter`, `creative_targeting`, `categories`, `persona`, `languages`, `market_target`, `niches`, `cta_type`, `link_url`. DCO ads are exploded into one row per card.
- `followed_brands` — brands being tracked for competitor ad monitoring. Simmer: `brand_id: n68cYDEnS6D6eU4T4bLS`, `page_id: 187701838409772`
- `foreplay_credit_log` — tracks Foreplay API credit usage per fetch call. Fields: `brand_id`, `page_id`, `credits_used`, `ads_fetched`, `credit_budget`, `start_date`, `stopped_reason`
- `video_analyses` — Primary record for video analysis. Fields: competitor_ad_id, run_id, video_url, duration_seconds, total_shots, total_cuts, avg_shot_duration, cuts_per_second, pacing_profile, transcript_text, ocr_text, combined_script, contact_sheet_url, ai_analysis (jsonb), layout_summary (jsonb, e.g. `{"full":6,"split-2":2,"split-3":0,"other":0}`), status, error_message. **Transcript observability (17 Apr 2026):** `transcript_status` (pending | running | partial | success | error, CHECK), `transcript_attempts` (int default 0), `transcript_error` (text), `transcript_completed_at` (timestamptz). Index `idx_video_analyses_transcript_status` on status WHERE NOT NULL. **OCR observability (17 Apr 2026):** `ocr_status` (pending | running | partial | success | error, CHECK), `ocr_attempts` (int default 0), `ocr_error` (text), `ocr_completed_at` (timestamptz). Index `idx_video_analyses_ocr_status` on status WHERE NOT NULL.
- `video_shots` — Individual shot records. Fields: video_analysis_id (FK), shot_number, start_time, end_time, duration, frame_url, ocr_text, description, screen_layout (text: `full` | `split-2` | `split-3` | `other`, nullable, CHECK constraint)
- `video_analysis_runs` — Batch analysis runs. Fields: brand_name, page_id, percentile (1/2/5/10/20), total_videos, analysed_count, status
- `followed_organic_accounts` — **(NEW Apr 2026, Phase 1.1)** Organic accounts followed on Instagram and YouTube. Fields: brand_name, platform (instagram | youtube, CHECK), handle, platform_account_id, uploads_playlist_id (YouTube only), is_active, fetch_frequency (default 'daily'), last_fetched_at, created_at. UNIQUE(platform, platform_account_id).
- `organic_posts` — **(NEW Apr 2026, Phase 1.1)** One row per organic post (reel, feed, short, long, carousel). Fields: account_id (FK, ON DELETE CASCADE), platform (CHECK), platform_post_id, post_url, post_type, video_url, thumbnail_url, title, caption, hashtags (text[]), posted_at, duration_seconds, audio_id, audio_title, language, raw (jsonb), first_seen_at, last_refreshed_at. **PR #36 (17 Apr 2026):** `thumbnail_cached_url text` nullable, Supabase-hosted mirror of the IG/FB CDN thumbnail, populated by `fetch-instagram-posts` on ingest and backfilled by `backfill-organic-thumbs`. UIs prefer this over `thumbnail_url` because IG/FB URLs are hotlink-blocked once the signed `oh=` token expires. UNIQUE(platform, platform_post_id). Indexed on (account_id, posted_at desc).
- `organic_post_metrics` — **(NEW Apr 2026, Phase 1.1)** Time-series metrics per post. Fields: post_id (FK, ON DELETE CASCADE), captured_at, views, likes, comments, saves, shares, engagement_rate. Indexed on (post_id, captured_at desc).
- `organic_fetch_log` — **(NEW Apr 2026, Phase 1.1)** Records every fetch run. Fields: account_id (FK, ON DELETE SET NULL), platform (CHECK), started_at, finished_at, posts_fetched, posts_new, cost_estimate (Apify USD, D4), yt_quota_units (YouTube API units, D4), status (running | success | error | partial, CHECK), error_message.

**Remaining Organic Intelligence work:** `video_analyses` will gain `organic_post_id` + `source_kind` columns (Phase 3) so the same pipeline serves ad and organic inputs. Decision log: `docs/organic-intel-decisions.md`.

### Supabase Storage Buckets

- `reference-images` — existing, public
- `static-uploads` — existing, public
- `video-processing` — public, 100 MB limit. Stores extracted video frames, contact sheets, and audio files. MIME types: video/mp4, video/webm, image/jpeg, image/png, audio/mp3
- `organic-thumbs` — **(NEW Apr 2026, PR #36)** public, 5 MB limit. Server-side mirror of IG/FB CDN thumbnails. MIME types: image/jpeg, image/png, image/webp. Object key convention: `{platform}/{platform_post_id}.jpg`. Populated by `fetch-instagram-posts` on ingest and `backfill-organic-thumbs` on demand. 522 objects at ship (371 IG + 151 YT).

### Workflow

1. Create a "run" with brand DNA (name, URL, product, extracted brand DNA)
2. Generate prompts across templates/categories
3. Create images from prompts
4. Review images (by AI or human) with ratings
5. Iterate on prompts based on feedback

## Key Commands

- **Dev:** `npm run dev` (Vite dev server)
- **Build:** `npm run build`
- **Deploy:** Push to `main` → Vercel auto-deploys production and `deploy-edge-functions.yml` redeploys any changed Supabase function.

## Branching and CI

See `docs/branching-and-ci.md` for the full flow. Quick summary:

- `main` → production (Vercel prod + edge function deploys)
- `develop` → integration (Vercel preview per push)
- `feature/<scope>` → off `develop`, PR into `develop`
- `hotfix/<scope>` → off `main`, PR into `main`, rebase `develop` after

CI workflow `ci.yml` runs `npm run build` on pushes and PRs to `main` and `develop`. Every PR must be green before merging. Edge-function deploys fire only from `main`. Branch protection rules are pending a user-side GitHub UI pass (tracked in Asana).

## Design System

- Dark theme (class="dark" on html element)
- Background: bg-bg-0
- Text: white
- Fonts: Inter (UI), JetBrains Mono (code/prompts)
- Antialiased rendering

## Current Status (18 April 2026)

- **Working:** Brand DNA extraction, prompt generation, image creation, review system, competitor ad viewer with inline video playback and Add Competitor button, Video Analysis Engine (Railway worker + 8 video edge functions), layout-aware UGC brief generation.
- **Phase 1 Complete:** Video Analysis Engine — Foundation (DB + pipeline + Railway worker + video edge functions). See `docs/video-analysis-project-spec.md`.
- **Phase 1 re-verification complete (16 Apr):** 24 deployed edge functions all have matching source in `supabase/functions/`. All 24 enforce `verify_jwt: true`. Both former JWT regressions (analyse-competitor-creatives, debug-auth) closed same day.
- **Organic Intelligence Phase 1.1 complete (17 Apr):** 4 new tables shipped with RLS (`followed_organic_accounts`, `organic_posts`, `organic_post_metrics`, `organic_fetch_log`). Migration `20260417064915_create_organic_intel_tables.sql`.
- **Organic Intelligence Phase 1.5 complete (17 Apr):** 3 CRUD endpoints live (`list-organic-accounts`, `save-organic-account`, `list-organic-posts`). End-to-end tested via anon key: upsert is idempotent against `UNIQUE(platform, platform_account_id)`, activate/deactivate toggles `is_active`, filters + pagination behave correctly.
- **Organic Intelligence Phase 2 complete (17 Apr):** Ingestion pipelines live for Instagram (Apify actor `apify/instagram-scraper`, ID `shu8hvrXbJbY3Eb9W`) and YouTube (Data API v3). 2 new edge functions (`fetch-instagram-posts` v4, `fetch-youtube-posts` v2) plus 20 accounts seeded (10 IG + 10 YT). End-to-end verified: idempotent upserts on `(platform, platform_post_id)`, metrics appended as time series, `last_fetched_at` updates, `organic_fetch_log` captures Apify cost (USD) and YouTube quota units. Shorts detection: `duration <= 60s` AND HEAD probe on `/shorts/{id}` with `redirect: manual`. See `docs/handover-2026-04-17-organic-intel-phase-2.md`.
- **Organic Intelligence Phase 3a complete (17 Apr):** Frontend `Organic Intel` tab shipped. `src/components/OrganicIntel.jsx` + `.css` ~820 lines total. List view shows all active IG+YT accounts with platform chip, last-fetched relative time, last-run status chip, and posts-tracked count; filter by All/IG/YT. Detail view shows header meta (last run, posts fetched/new, cost or YT units), totals bar (views/likes/comments/count), and up to 50 most recent posts as cards (thumbnail, post_type chip, duration badge for videos, caption, hashtags, posted date, latest metrics, links to post_url + video_url). Data fetched via PostgREST direct reads; latest-log + per-account post counts grouped client-side (to be swapped for an RPC once volumes grow). Live and verified on `creative-kitchen-static.vercel.app`.
- **Organic Intelligence Phase 3b complete (17 Apr):** Supabase RPC `list_organic_accounts_with_stats(p_platform, p_active_only)` joins `followed_organic_accounts` to its latest `organic_fetch_log` row plus `organic_posts` count. Frontend `OrganicIntel.jsx` `loadAll` now calls the RPC in one round trip (replacing three PostgREST reads + client-side grouping). Migration `supabase/migrations/20260417100000_create_list_organic_accounts_with_stats_rpc.sql`. `security invoker`, execute granted to anon/authenticated/service_role. Verified live in bundle `/assets/index-BZ0kQLUG.js`.
- **Organic Intelligence Phase 3c complete (17 Apr):** Scheduled ingestion shipped end-to-end. Vault secret `organic_cron_service_key` populated, orchestrator auth fix deployed (v4 / 1.1.0), and both IG + YT smoke tests passed end-to-end via the cron helper path. Cron jobs armed 17 Apr 2026 (all three `active = true`): IG daily at 02:15 UTC, YT morning 06:30 UTC, YT evening 18:30 UTC. Orchestrator now forwards the caller's `Authorization` header to fetchers (and supports optional `ORG_CRON_SERVICE_KEY` env override) because auto-injected `SUPABASE_SERVICE_ROLE_KEY` is now `sb_secret_...` format which PostgREST accepts but `verify_jwt: true` on the gateway rejects. New orchestrator edge function `trigger-organic-fetches` (v4, 1.1.0) with per-platform budget guards (IG $1.00/UTC-day, YT 8000 units/UTC-month), due-account filter on `last_fetched_at`, and staggered dispatch to `fetch-instagram-posts` / `fetch-youtube-posts`. Migration `20260417140000_phase3c_cron_schedule.sql` installs pg_cron, adds SECURITY DEFINER helper `public._trigger_organic_platform(text)` that reads the service role JWT from `vault.decrypted_secrets` (secret name `organic_cron_service_key`) and POSTs via `net.http_post`, and schedules three jobs: `organic_fetch_instagram_daily` at 02:15 UTC, `organic_fetch_youtube_morning` at 06:30 UTC, `organic_fetch_youtube_evening` at 18:30 UTC, all `active = false` on ship. Observability RPC `public.list_fetch_runs_summary(p_since)` rolls up `organic_fetch_log` into daily per-platform rows (runs, status breakdown, posts_fetched/new, cost_estimate, yt_quota_units). UI strip `.oi-runs-strip` on Organic Intel tab renders a "Last 7 days" chip per platform from that RPC. Verified end-to-end via `net.http_post` → orchestrator → fetcher: IG `allplants` 1 post (cost $0.046 of $1.00), YT `aragusea` 1 post (4 of 8000 quota units). `organic_fetch_log` rows written for both. See `docs/handover-2026-04-17-organic-intel-phase-3c.md`.
- **VAE polymorphic source + Trend Reports shipped (17 Apr):** `video_analyses` now supports polymorphic source (`source`='competitor_ad'|'organic_post', `source_id` text) alongside legacy `competitor_ad_id` for back-compat. `analyse-video` v7 (2.0.0) accepts `{source, source_id}` or legacy `{competitor_ad_id}`, resolves `video_url` from the right table, duplicate-guards both paths. `list-video-analyses` v8 (1.2.0) filters + batch-enriches organic analyses with `organic_posts` + `followed_organic_accounts`. `get-video-analysis` v7 (1.1.0) attaches `organic_post.account` on organic sources. `VideoAnalysis` UI gained an All/Ads/Organic filter bar with per-card source chip; `OrganicIntel` post cards gained an Analyse button + analysed chip.
- **Trend Reports shipped (17 Apr):** New `trend_reports` table drives cross-video pattern synthesis. Three new edge functions (all v1, verify_jwt: true): `synthesise-organic-trends` accepts `{ids}` or `{filter}` (source/platform/pacing_profile/duration bounds/since_days/brand_name/limit), enriches analyses with sources + shots, builds a formatted corpus (transcript 600c, caption 280c, up to 10 shots with layout + duration), calls Claude Sonnet 4.5 (8192 max_tokens) with a strict JSON schema (overview, recurring_hooks, shot_length_stats, layout_mix, recurring_phrases, audio_reuse, ctas, themes, production_notes, copy_ideas). Writes the row `status='running'` up front so partial failures persist `error_message`. Min 3 analyses. `dry_run: true` returns prompt preview. `list-trend-reports` + `get-trend-report` for UI consumption. Frontend: new `Trend Reports` tab (list + detail views) with generator form. Requires `CLAUDE_API_KEY` or `ANTHROPIC_API_KEY` secret on `synthesise-organic-trends`.
- **Trend Reports v1.1.0 shipped (17 Apr, PR #30):** Top-performers filter + Ideas output + thumbnail fallback. `synthesise-organic-trends@1.1.0` (v3, ACTIVE) adds filter fields `top_performers` (bool), `rank_by` (views | engagement_rate | likes | comments | shares), `top_n` (3 to 40); when enabled it queries `organic_post_metrics` for the latest snapshot per post, ranks organic analyses DESC by the chosen metric, and passes top-N to Claude with a "why did these win" framing (each corpus block gets a `metric(rank_by): N` line so the model can cite actual numbers). Claude output now includes `actionable_ideas[]` with priority 1-3, title, rationale, format, target_duration, pacing, layout, hook, beats, CTA, and `[index]` back-refs to winners; top mode also emits `why_these_won[]` (3 to 6 diagnostic bullets). `get-trend-report@1.1.0` (v3, ACTIVE) attaches `first_frame_url` per source (Supabase-hosted `video_shots` row where `shot_number=1`) plus `performance_metric_value`. Frontend `TrendReports` gained an `IdeaCard` with priority-coloured cards (P1 green, P2 amber, P3 grey), `formatMetric` helper, and a thumb fallback chain that prefers Supabase over IG/FB CDN (`referrerPolicy="no-referrer"` + `onError` placeholder as final hedge) since IG/FB `oh=` signed URLs are hotlink-blocked. Commit `1b84c20` on `main`; Vercel production deploy `dpl_FUFAiNBeydpNhNJKK9YoX5XawzH3` READY.
- **Trend Reports UX + desktop nav fix shipped (17 Apr, PR #31):** `.nav-tabs` now `flex-wrap: wrap` + `overflow-x: auto` on desktop (was only mobile), `.nav-tab` pinned with `flex-shrink: 0` + `white-space: nowrap` so all 14 tabs stay reachable on narrower laptop viewports. `TrendReports` generator gained a debounced PostgREST HEAD `count=exact` preflight (350ms) against `video_analyses` using the same filters (source, pacing, since_days, duration range) — shows `N analysed videos match (before platform/brand filter)` with bad (<3) / warn (<10) / good (≥10) colour states, disables submit when count < 3, and renders contextual rescue actions ("Switch to competitor ads", "Source: any", "Extend to 180 days", "Any pacing"). Source-aware copy nudges users toward the 70-analysis competitor_ad corpus when they hit the organic-post shortage (currently only 1 organic analysis exists). Commit `a735d4e` on `main`; Vercel production deploy `dpl_31M8QeF5dojBRU2L2FJHGRF7TcLs` READY.
- **VAE Phase 2 Whisper subtask complete (17 Apr):** `transcribe-video` v5 hardened with retry + partial-text detection + DB observability (migration `20260417141000_phase2_whisper_observability_columns.sql` applied; 70 existing rows backfilled to status 'success'). Frontend `CompetitorAds.jsx` `runPipelineSteps` wraps transcribe-video with 2 client-side retries. Smoke-tested end-to-end. First real validation of the PR #5 `deploy-edge-functions.yml` shallow-clone fix, the workflow correctly diffed one changed file and deployed only `transcribe-video`. Asana task `1214111674112796` closed. See `docs/handover-2026-04-17-whisper-retry-observability.md`.
- **VAE Phase 2 OCR subtask complete (17 Apr, PR #33):** `ocr-video-frames` v10 mirrors the Whisper v5 hardening pattern. Per-batch retry (3 attempts, 1s/3s/7s backoff, transient-only via `ClaudeError.transient` — 429/529/5xx/transport transient; 4xx and JSON-parse failures non-transient). Batch-level continue-on-failure: `batchErrors[]` tracks per-batch outcomes so one failed batch does not discard the others. Partial detection: `coverage < 0.9` OR any `batch_errors` flags run as `partial` (not retried). Preserves prior `ocr_text` on partial regressions. Full DB observability via 4 new columns on `video_analyses` (`ocr_status`, `ocr_attempts`, `ocr_error`, `ocr_completed_at`). `x-function-version: ocr-video-frames@10` response header. Frontend `CompetitorAds.jsx` `runPipelineSteps` wraps ocr-video-frames with 2 client-side retries (2s delay) for gateway blips and logs new observability fields on success. Migration `20260417170000_phase2_ocr_observability_columns.sql` applied, 69 existing rows backfilled to status 'success' (4 remain NULL, no `ocr_text` yet). Smoke-tested end-to-end against a 5-shot analysis: `ocr_status=success`, `shots=5/5`, `coverage=1`, `batch_errors=0`, `attempts=1`, `ocr_completed_at` updated. Deploy workflow correctly diffed 1 changed function and deployed only ocr-video-frames. Asana task `1214111637586477` ready to close.
- **Organic Intel UI thumbnail fix + bulk analyse shipped (17 Apr, PR #35):** `src/components/OrganicIntel.jsx` (+264/-26, now 793 lines) + `src/components/OrganicIntel.css` (+143). Thumbnail fix mirrors Trend Reports v1.1.0 pattern: `AccountDetail` fetches `video_shots?shot_number=eq.1` per analysed post in `refreshAnalysesForPosts` and passes `firstFrameByPostId` down to `PostCard`. Thumb chain is Supabase `frame_url` → original `post.thumbnail_url` with `referrerPolicy="no-referrer"` → `onError` reveals `.oi-thumb-missing` "Preview blocked" placeholder. Fixes IG/FB `scontent-*.cdninstagram.com` and `instagram.*.fbcdn.net` hotlink blocking (signed `oh=` URLs + referrer checks); YouTube `i.ytimg.com` was already fine. Bulk analyse: new `.oi-bulk-bar` above the grid with Select all / Clear / `Analyse N selected`, per-card `.oi-select-box` checkbox overlay (top-left of thumbnail), amber `.oi-post-selected` border, thumbnail chips `.oi-chip-bulk-{queued|running|done|error}`. Eligibility = `post.video_url` AND `post_type ∈ {reel, short, video}` AND not already analysed. `BULK_CONCURRENCY = 3`: three async workers share a cursor, each calling `analyse-video` with `{source: 'organic_post', source_id: post.platform_post_id}`. Per-card `analysing` prop merges bulk running state so the single-post button still works. Bulk bar hides when `eligibleCount === 0`. Smoke-tested live on calo.uk detail view: "Preview blocked" placeholders correctly replace 3 IG CDN thumbnails while the analysed Reel renders its Supabase first_frame_url; Select all → "35 selected of 35 eligible videos" with primary CTA enabled; Clear → 0 selected, CTA disabled. Squash commit `f9ebef3` on `main`; Vercel production deploy `dpl_GdqX5sSttDKyBH4UM45rpaetsoXQ` READY. See `docs/handover-2026-04-17-organic-intel-thumb-bulk-analyse.md`.
- **VAE Phase 2 generate-ugc-brief subtask complete (17 Apr, PR #34):** `generate-ugc-brief` v7 wires transcript + per-shot OCR into the Claude prompt. Fetches `video_shots` for the analysis, builds a `REFERENCE VIDEO CONTEXT` block with the verbatim transcript (head+tail truncated at 1400 chars) and per-shot OCR aligned to `M:SS-M:SS` timestamps plus layout markers. System prompt now requires shot 1's `script_line` to quote the opening spoken hook word-for-word when a transcript is available, and to mirror recurring OCR beats in Chefly's `text_overlay` fields. Graceful Phase 1 fallback when transcript/OCR missing. `FUNCTION_VERSION = "generate-ugc-brief@7"` with `x-function-version` response header; success payload carries a new `context` object (`transcript_available`, `transcript_status`, `transcript_chars`, `ocr_available`, `ocr_status`, `shots_with_ocr`, `total_shots`). Smoke-tested against 4 analyses: Calo (697-char transcript) → shot 1 quoted opening line verbatim; Your Heights (rap-lyrics transcript) → correctly sourced hook from persistent OCR instead of music lyrics; Annie Openshaw (FODMAP monologue) → shot 1 quoted opening line verbatim; Phase 1 fallback (no transcript/OCR) → produced a sensible hook in the documented style. Asana task `1214111637546592` ready to close.
- **Organic Intel thumbnail cache + percentile selector shipped (17 Apr, PR #36):** Three-layer defence against IG/FB CDN hotlink blocking (`oh=` signed URLs + IP/UA crosscheck): (1) new `organic_posts.thumbnail_cached_url` column, (2) public `organic-thumbs` Supabase bucket (5 MB, image/*), (3) snapshot-on-ingest in `fetch-instagram-posts@1.1.0` (v5) that fans out at concurrency 3 and patches the row. `backfill-organic-thumbs` v1 cleared the 522-row pre-existing backlog (371 IG + 151 YT, 0 failures). Runtime proxy fallback `proxy-thumbnail` v1 handles anything that slips through. UI thumb chain is now `firstFrameUrl → thumbnail_cached_url → buildProxyThumbUrl(thumbnail_url) → placeholder`; hotlink-blocked hosts are routed through the proxy only when the mirror hasn't landed. Percentile bulk selector added to the `.oi-bulk-bar`: 2.5/5/10/20% pills rank `selectablePosts` by `metricsByPost[id].views desc` (zero network cost; metrics already loaded) and select `ceil(eligibleCount * pct / 100)` posts with a min of 1. On calo.uk 35-eligible pool: 2.5%=1, 5%=2, 10%=4, 20%=7. Migration `20260417180000_organic_thumbnails_cache.sql` applied. Commit `6973398` on `main`; Vercel production deploy `dpl_FhpYruzhhf2sHz4ziuiwSBFzRRec` READY. See `docs/handover-2026-04-17-organic-intel-thumb-proxy-percentile.md`.
- **Organic Intel pipeline + percentile visibility fix shipped (17 Apr, commit `4dd01b5`):** Three fixes to the Organic Intel tab in `src/components/OrganicIntel.jsx` (+144/-3) + `.css` (+9). (1) `onlySelected` state + `displayPosts` useMemo filters the grid to the selected ids when a percentile pill is active, and sorts by latest views desc whenever a percentile is active, so the user can see exactly which posts were picked. New "Only selected" / "Show all" toggle in the bulk bar (`oi-bulk-btn-active` highlight) lets the user switch back to the full grid without losing the selection. (2) New `runPipelineSteps(analysisId)` mirrors `CompetitorAds.jsx`: after `analyse-video` (Phase 1) succeeds or returns 409, drives `transcribe-video` → `ocr-video-frames` → `merge-video-script` → `ai-analyse-video` with 2 client-side retries on the first two steps (retryable = 429 or 5xx, 2s delay). `analyseOne` returns the extracted `analysis_id` and wraps the pipeline in try/catch so one failure doesn't abort a bulk run; console logs `transcript_status`, `ocr_status`, coverage and batch_errors on success. (3) Backfilled the 8 pre-existing stale `organic_post` analyses end-to-end (transcribe/OCR/merge/ai-analyse) so the Trend Reports corpus is no longer dead: all 8 now have `transcript_status=success`, `ocr_status=success`, `combined_script` and `ai_analysis` populated. Regenerated Trend Report `a0a1d29f-a744-4899-ba1a-22ecf7507044`: `status=complete`, `source_count=8`, `model=claude-sonnet-4-5-20250929`, all 11 summary sections (including `actionable_ideas`) populated; Claude correctly synthesised the Calo UK × Anjula Devi chef-heritage narrative across the 8 videos. Verified with esbuild jsx syntax check (zero errors, 36.2kb). Local `npm run build` skipped due to `ENOSPC` on the session volume; Vercel CI built + deployed clean. Commit `4dd01b5` on `main`; Vercel production deploy `dpl_2nFFm9PNMgq7DTV46FjuM2hGaPzk` READY. See `docs/handover-2026-04-17-organic-intel-percentile-pipeline.md`.
- **Organic Intel pipeline status chips + backfill script shipped (17 Apr, PR #37):** Two follow-ups from the 4dd01b5 fix. (1) `src/components/OrganicIntel.jsx` now surfaces `transcript_status`, `ocr_status` and `ai_analysis` on each analysed post card as four compact chips (`Analysed`, `TX ok/partial/error/running/pending`, `OCR ...`, `AI`) next to the existing type + duration badges. PostgREST `video_analyses` select extended to pull those columns; thumb badge row gets `flex-wrap` + `row-gap` so the extra chips fit at narrow widths. Chips reuse `.oi-chip-status` colour tokens (success = green, partial = amber, error = red, running = blue, pending = grey). New `.oi-chip-ai` class for the AI marker. (2) `scripts/backfill-organic-pipeline.mjs` replaces the ad-hoc curl sequences used on 17 Apr to heal 8 stale organic_post analyses. Serial by default, `--concurrency=N` to raise, `--dry-run` to preview, `--analysis-id=<uuid>` to target one row, `--source=organic_post|competitor_ad` + `--limit=N` for batch mode. Idempotent (edge functions dedup against their status columns); uses the same retry policy (2 retries on 429/5xx for transcribe + OCR, none for merge/ai) as the frontend `runPipelineSteps`. Verified clean via esbuild jsx parse (38 KB bundle, 0 errors) and `node --check` on the backfill script. Commit `345e782` on `main`; Vercel production deploy `22YDHSsDnM6FQ21erFyenAX5Afkf` READY; live bundle `/assets/index-Cwrgr5Sw.js` contains `PipelineStatusChip`, `transcript_status`, `ocr_status`, `oi-chip-ai`. See `docs/handover-2026-04-17-organic-intel-status-chips.md`.
- **Organic Intel stable percentile + bulk-run memory shipped (17 Apr, PR #38):** Fixes two UX regressions reproduced against `calo.uk`. (1) Top percentile selection was unstable across visits because both `selectTopPercentile` and the `displayPosts` memo used inline `sort((a,b) => bv - av)` with no tie-breaker; when fresh `organic_post_metrics` rows landed between visits (20 calo.uk snapshots dropped at 18:30:03 UTC mid-session), tied view counts reshuffled silently. New top-level helper `makePostViewComparator(metricsByPost)` enforces `views desc to posted_at desc to id asc`, used by both call sites. (2) Bulk Analyse state vanished on unmount because `bulkStatuses` was component-local; opening a video to watch it blew away the map even though server-side runs (12 calo.uk organic-post analyses succeeded in 2h) completed fine. New per-account localStorage record at `oi.lastBulkRun.v1` (shape `{[accountId]: {startedAt, finishedAt, queuedIds, succeeded, failed}}`, 24h TTL) persists the last run; hydrated lazily via `useState(() => readLastBulkRun(account.id))`. Return-visit banner renders above the bulk bar with `N completed, M errors, K posts` + relative time, plus two actions: **View last batch** (restores `selectedIds` to `queuedIds`, clears percentile, enables `onlySelected`) and **Dismiss** (clears the record). `.oi-last-run-banner` uses a subtle info palette (`rgba(92, 207, 255, 0.08)` bg / `0.25` border) so it doesn't compete with the live progress strip; hidden during `bulkRunning`. Helpers guard against missing accountId, private-mode storage denial, malformed JSON, and stale records. esbuild JSX + CSS syntax check clean. Commit `b296e1e` on `main`; Vercel production deploy `32khb8i2u4Pu6Batv4Th7oKckgq6` READY; live bundle `/assets/index-B7-sZ4gi.js` contains `oi-last-run-banner`, `oi.lastBulkRun.v1`, `Last Analyse run`, `lastBulkRun`, `finishedAt`, `queuedIds`, `View last batch`. See `docs/handover-2026-04-17-organic-intel-stable-percentile-run-memory.md`.
- **Mixed → Named Format Combos migration complete (18 Apr, PR #42):** `ai-analyse-video` v3 (edge function version 5, ACTIVE, CI-deployed entrypoint) replaces the terminal `production_style.format = "mixed"` enum with a 4-field taxonomy written additively into the `ai_analysis` JSONB: `primary_format` (required, from `ugc | talking-head | studio | lifestyle | animation | b-roll-heavy | screen-recording`, no `mixed`), `secondary_format` (optional, only when ≥20% of runtime and distinct), `format_label` (human-readable combo, e.g. "UGC + Talking Head"), `format_rationale` (one sentence, ≤180 chars, describing the runtime split). `normaliseProductionStyle()` guard coerces any Claude drift (converts stray `"mixed"` to null, auto-builds `format_label` from primary+secondary, mirrors label back into legacy `format` key so pre-v3 callers keep working). `x-function-version: ai-analyse-video@3` response header. UI updated: `src/lib/shareableExport.js` `renderProductionStyle` reads `format_label || format` and renders `format_rationale` as italic prose under the metric table; `src/components/VideoAnalysis.jsx` AnalysisTab accepts both legacy (`overlays`/`music`) and new (`text_overlays`/`music_pacing`) keys. All 20 legacy `mixed` rows (18 competitor_ad + 2 organic_post) re-run through v3; sanity queries report 0 rows with `format = 'mixed' AND primary_format IS NULL`, 0 rows with missing `format_label`, and a healthy distribution (no bucket >30%). Top combos: UGC + Lifestyle (6), Talking Head + B-Roll Heavy (4), Talking Head + Lifestyle (3). See `docs/mixed-format-migration-2026-04-18.md` + `scripts/reclassify-mixed-legacy.sql`. Asana `1214120254371872` closed. **Follow-up:** `develop` drifted ~37 commits behind `main` from direct hotfixes, so this PR was cherry-picked onto a release branch off `main` via PR #42 (PR #40 merged to develop; PR #41 closed). Resync develop with main separately.
- **Next:** Resync `develop` with `main` to close the hotfix-drift gap (tracked in Asana). Monitor cron runs (IG 02:15 UTC tomorrow, YT 06:30 UTC tomorrow) via the Organic Intel Last-7-days strip + `organic_fetch_log`. Verify `proxy-thumbnail` invocation count trends toward zero now that the mirror + backfill cover 522/522 rows. Still deferred: rotate Apify + YouTube API keys (both leaked earlier in session). VAE Phase 2 epic fully shipped 17 Apr — close Asana `1214111637586477` (OCR) and `1214111637546592` (generate-ugc-brief v7). Hard-delete `debug-auth` once caller logs confirm no traffic for 7 days (~23 Apr). Stale duplicate `src/CompetitorAds.jsx` still sitting for Phase 3 cleanup. Low-priority: mirror YT thumbnails on ingest in `fetch-youtube-posts` for parity (YT is not hotlink-blocked today, so insurance only).
- **Last deployed:** 18 April 2026.
- **generate-ad-prompt:** v29 (packaging-aware, dynamic packaging terms).
- **fetch-competitor-ads:** v12 (brand_id/page_id, DCO explosion, credit logging to `foreplay_credit_log`, default `start_date: 2025-12-23`, `credit_budget: 500`).
- **analyse-competitor-creatives:** v32 (JWT re-enabled 16 Apr).
- **debug-auth:** v6 (soft-retired 16 Apr, returns HTTP 410 Gone; hard-delete tracked in Asana).
- **ai-analyse-video:** v3 (18 Apr: named format combos — `primary_format` / `secondary_format` / `format_label` / `format_rationale` replace the terminal `"mixed"` enum; `normaliseProductionStyle()` guard; `x-function-version: ai-analyse-video@3` header; layout detection via Claude vision retained).
- **generate-ugc-brief:** v7 (Phase 2: transcript + per-shot OCR context in Claude prompt, verbatim hook quoting, graceful Phase 1 fallback, x-function-version header, 16384 max_tokens, shot variations 2/3/4, layout-aware prompts).
- **brand_guidelines table:** packaging_format, packaging_specs, colour_palette, typography, tone_of_voice, photo_descriptions columns live.
- **Foreplay API:** `public.api.foreplay.co`, key stored in edge function. 1 credit per ad. Simmer brand_id: `n68cYDEnS6D6eU4T4bLS`.
- **Supabase anon key:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imlmcnh5bHZvdWZuY2R4eWx0Z3F0Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzM4MzkwNDgsImV4cCI6MjA4OTQxNTA0OH0.ZsyGK_jdxjTrO3Ji8zgoyHz6VxW5hR36JWr1sgmmAFA`

## Edge Functions

**32 edge functions deployed.** All 32 enforce `verify_jwt: true` (24 verified 16 Apr; +3 Organic Intelligence CRUD endpoints on 17 Apr; +2 Organic Intelligence fetchers on 17 Apr; +1 Organic Intelligence orchestrator `trigger-organic-fetches` on 17 Apr; +2 Organic Intelligence thumbnail utilities on 17 Apr via PR #36).

**Prompt / brand / image tooling:**

1. `generate-ad-prompt` — v29. Packaging-aware prompt engine with dynamic packaging terms.
2. `refine-prompt` — v6. Surgical prompt editing.
3. `templatize-prompt` — v7. Converts prompts into reusable templates.
4. `compare-prompts` — v5. Visual diff between two prompts.
5. `generate-variables` — v9. Meal-specific creative variables.
6. `extract-brand-guidelines` — v8. Parses brand docs into structured JSON.
7. `describe-photo` — v11. Photo library descriptions via Claude.
8. `generate-shot-sequence` — v5. Food photography shot sequence generator.

**Ad library / competitor analysis:**

9. `fetch-competitor-ads` — v12. Foreplay API ingestion with credit budgeting and DCO card explosion.
10. `seed-advertisers` — v4. Bootstrap advertiser seed data.
11. `extract-ad-thumbnails` — v9. Extracts images from ad HTML snapshots.
12. `analyse-competitor-creatives` — v32. Multi-step AI visual analysis pipeline. JWT re-enabled 16 Apr.
13. `vision-model-test` — v5. Standalone visual forensic analysis test.
14. `process-analysis-batch` — v24. Batch orchestrator for vision analysis.

**Video analysis pipeline:**

15. `analyse-video` — v4. Orchestrator; accepts competitor_ad_id, calls Railway worker, writes `video_analyses` + `video_shots`. Secrets: `VIDEO_WORKER_URL`, `VIDEO_WORKER_SECRET`.
16. `list-video-analyses` — v5. Query analyses with filters (status, run_id, competitor_ad_id) and pagination. Exposes `X-Function-Version: list-video-analyses@1.1.0` response header (CI deploy audit marker).
17. `get-video-analysis` — v4. Fetch single analysis with all shots + full competitor ad context.
18. `transcribe-video` — **v5 (17 Apr 2026)**. Whisper transcription leg of the pipeline with retry + partial-text detection. Retry: 3 attempts, backoff 1s/3s/7s, transient only (429/5xx/transport) via `WhisperError.transient`. Partial detection: `coverage < 0.9 AND duration >= 5s` flags as `partial` (not retried; audio quality issue). Writes `transcript_status` (running → success | partial | error), `transcript_attempts`, `transcript_error`, `transcript_completed_at`. Response body includes `transcript_status`, `coverage`, `attempts`, `duration_seconds`.
19. `ocr-video-frames` — **v10 (17 Apr 2026)**. Claude-vision OCR leg of the pipeline with per-batch retry + partial detection + DB observability. Reads `video_shots` rows (shots with `frame_url`), batches 8 frames per Claude Sonnet 4.6 vision call, writes OCR text + description per shot, aggregates combined `ocr_text` on video_analyses. Retry: 3 attempts per batch, backoff 1s/3s/7s, transient only (429/529/5xx/transport) via `ClaudeError.transient`; 4xx and JSON-parse failures non-transient. Batch-level continue-on-failure: `batchErrors[]` preserves successful batches when one fails. Partial detection: `coverage < 0.9` OR any batch_errors flags run as `partial` (not retried). Writes `ocr_status` (running → success | partial | error), `ocr_attempts`, `ocr_error`, `ocr_completed_at`. Response body includes `ocr_status`, `coverage`, `shots_updated`, `batch_errors`, `total_batches`, `attempts`. Response header: `x-function-version: ocr-video-frames@10`. Secrets: `CLAUDE_API_KEY` or `ANTHROPIC_API_KEY`.
20. `merge-video-script` — v1. Merges transcript + OCR into combined script.
21. `extract-video-script` — v2. Orchestrates transcribe → OCR → merge.
22. `ai-analyse-video` — **v3 (18 Apr 2026)**. Creative strategy analysis + layout detection via Claude Sonnet 4.6 vision. Writes `ai_analysis` JSONB (hook, narrative_arc, cta, selling_points, emotional_drivers, target_audience, production_style, pacing_analysis, competitor_insights, one_line_summary, shot_layouts) and mirrors shot_layouts into `video_shots.screen_layout` + `video_analyses.layout_summary`. **v3 taxonomy change:** `production_style` now emits `primary_format` (required, 7-value vocab, no `mixed`), `secondary_format` (optional, ≥20% runtime), `format_label` (human-readable combo), `format_rationale` (≤180 char one-sentence split). `normaliseProductionStyle()` guard coerces Claude drift; mirrors `format_label` back into legacy `format` key for pre-v3 readers. Response header `x-function-version: ai-analyse-video@3`. Secrets: `CLAUDE_API_KEY` or `ANTHROPIC_API_KEY`.
23. `generate-ugc-brief` — v7. Chefly-branded UGC creator briefs with Phase 2 transcript + per-shot OCR context (verbatim hook quoting, graceful Phase 1 fallback, `x-function-version` response header). 16384 max_tokens, truncation detection, shot variations (2/3/4), layout-aware prompts. Secrets: `CLAUDE_API_KEY` or `ANTHROPIC_API_KEY`.

**Organic Intelligence — CRUD (Phase 1.5):**

25. `list-organic-accounts` — v1. List followed IG + YouTube accounts with `platform` / `is_active` filters, pagination, and exact count. GET + POST. Ordered `brand_name.asc, platform.asc`.
26. `save-organic-account` — v1. Upsert / activate / deactivate a `followed_organic_accounts` row. POST only. Upsert uses PostgREST `on_conflict=platform,platform_account_id` against the UNIQUE constraint; activate/deactivate takes `id` or `(platform, platform_account_id)` and toggles `is_active` without deleting.
27. `list-organic-posts` — v1. Query `organic_posts` with filters `account_id`, `platform`, `post_type`, `language`, `posted_after`, `posted_before`, plus pagination. Ordered `posted_at desc nullslast` to match `organic_posts_account_posted_idx`. GET + POST.

**Organic Intelligence — Ingestion (Phase 2):**

29. `fetch-instagram-posts` — **v5 @ 1.1.0 (17 Apr 2026, PR #36)**. Pulls up to N recent posts for a handle via Apify actor `apify/instagram-scraper` (ID `shu8hvrXbJbY3Eb9W`). Modes: `test` (no writes, returns sample) and `fetch` (writes to `organic_posts` + `organic_post_metrics`, updates `last_fetched_at`, logs to `organic_fetch_log` with status `success|error|partial` and `cost_estimate` in USD ~$2.30/1000 results). Upsert uses `on_conflict=platform,platform_post_id`. Detects post_type (`image|carousel|reel`) from actor payload. **New in 1.1.0:** snapshot-on-ingest: after every upsert cycle, fans out (concurrency 3) to download each post's `thumbnail_url` server-side (Supabase runtime IP + Safari UA, 10s timeout, 5 MB cap, image/* guard), uploads to `organic-thumbs/instagram/{platform_post_id}.jpg` with `x-upsert: true`, patches `thumbnail_cached_url`. Best-effort: failures counted (`thumbnails_failed`) but never fail the run. Response payload gains `thumbnails_snapshotted` / `thumbnails_failed`; `X-Function-Version: fetch-instagram-posts@1.1.0` header. Secrets: `APIFY_TOKEN`.
30. `fetch-youtube-posts` — v2. Pulls up to N recent uploads for a channel via YouTube Data API v3 (`playlistItems.list` + `videos.list` on snippet, contentDetails, statistics → 2 quota units per fetch). Uploads playlist resolved from `channel UC` → `UU`. Shorts detection: duration ≤ 60s AND HEAD probe on `https://www.youtube.com/shorts/{id}` with `redirect: manual` (2xx = Short; Location containing `/shorts/` = Short; redirect to `/watch` = regular video). post_type values: `short|video|livestream`. Monthly quota budget 10,000 units (configurable) with 80% warning. Modes: `test` and `fetch`. Secrets: `YOUTUBE_API_KEY`.

**Organic Intelligence — Orchestration (Phase 3c):**

31. `trigger-organic-fetches` — v2. Scheduled orchestrator called by pg_cron via `public._trigger_organic_platform(text)`. Request body `{platform, idle_hours?, limit_per_account?, stagger_ms?, max_accounts?, dry_run?}`. Pre-flight budget guard aggregates `organic_fetch_log` since UTC day (IG) or UTC month (YT) and bails with `status: budget_exhausted` or `quota_exhausted` before dispatching. Picks due accounts (active, `last_fetched_at IS NULL` OR older than `idle_hours`) capped at `max_accounts` (default 50), POSTs to `fetch-instagram-posts` or `fetch-youtube-posts` with `stagger_ms` spacing (default 30s). Per-account errors surface in the aggregate but do not fail the run. `dry_run: true` returns the plan with zero side effects.

**Organic Intelligence — Thumbnails (PR #36, 17 Apr):**

32. `proxy-thumbnail` — v1 @ 1.0.0. Runtime fallback for IG/FB CDN hotlink-blocked thumbnails. GET `/functions/v1/proxy-thumbnail?url=<encoded>`. Strict host allow-list (`scontent*.cdninstagram.com`, `instagram.*.fbcdn.net`, `*.fbcdn.net` variants), returns upstream image bytes with `Cache-Control: public, max-age=86400, s-maxage=86400`. 400 on malformed / disallowed URL; 502 on upstream refusal (expired `oh=` token). `verify_jwt: true`. No SSRF surface beyond the allow-list.
33. `backfill-organic-thumbs` — v1. POST-only one-off helper. Body `{platform?: "instagram"|"youtube" default "instagram", batch?: 1-100 default 25, dry_run?: bool}`. Selects `organic_posts` where `thumbnail_cached_url is null and thumbnail_url is not null`, ordered `first_seen_at desc`, downloads bytes, magic-byte content-type detection (0x89,0x50 = PNG; 0x52,0x49 + 0x57@8 = WebP; else JPEG), uploads via `x-upsert: true` to `organic-thumbs/{platform}/{platform_post_id}.jpg`, patches the row. Used 17 Apr to clear the 522-row pre-PR-#36 backlog (371 IG + 151 YT, 0 failures).

**Diagnostics:**

28. `debug-auth` — v6. **Soft-retired 16 Apr.** Returns HTTP 410 Gone with a retirement notice. Source stays in the repo until callers confirmed gone; then hard-deleted.

**Alignment:** Every deployed slug has a matching `supabase/functions/<slug>/index.ts` directory on `main` (verified 16 Apr). A previous CLAUDE.md revision listed `sync-competitor-metadata` as deployed; that entry was incorrect and has been removed.

**CI deploy audit signal:** Functions deployed via `deploy-edge-functions.yml` show an `entrypoint_path` of `file:///home/runner/work/...` in `list_edge_functions`. Manual / dashboard deploys show `file:///tmp/user_fn_.../source/index.ts`. Only `list-video-analyses` currently carries the runner path; the rest predate CI.

## Video Worker (Railway Microservice)

Located at `video-worker/` in repo. Express + FFmpeg service for heavy video processing.

- **Endpoint:** `POST /process-video` — downloads video, detects shots via FFmpeg scene analysis, extracts reference frames, generates contact sheet, extracts audio.
- **Auth:** Bearer token via `WORKER_SECRET` env var.
- **Deployment:** Railway with Docker (Dockerfile in repo). Project: `triumphant-dedication`, URL: `https://creative-kitchen-static-production.up.railway.app`.
- **Env vars:** `WORKER_SECRET`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `PORT=3000`.
- **Status:** Deployed and verified end-to-end (13 Apr 2026). FFmpeg 5.1.8. Contact sheets tested on 17+ shot videos.
- **Test Videos:**
  - A: Simmer `3324195914449903` — 12.7s, 8 shots, 720x900
  - B: Huel `33860239276954284` — 21.4s, 17 shots, 720x1280
  - C: Frive `1440540640941645` — 31.4s, 17 shots, 720x1280

**Planned:** yt-dlp added to the worker image to support YouTube downloads for the Organic Intelligence phase.

## Development Rules

- **GitHub is the single source of truth.** If it's not in the repo, it doesn't exist.
- **Edge functions deploy FROM the repo.** Never deploy directly to Supabase from session code. Commit to GitHub first.
- **Every deployed slug must have a matching `supabase/functions/<slug>/index.ts`.** Re-verified 16 Apr; now 1:1.
- **verify_jwt: true is the default.** Never deploy a function with verify_jwt: false without an open ticket explaining why and a dated remediation plan.
- **No direct pushes to main.** Use `feature/<scope>` → PR to `develop` → PR to `main`. Hotfixes PR direct to `main`, then rebase `develop`. See `docs/branching-and-ci.md`.
- **Ticket-first for multi-file changes.** If a change touches more than one file, create an Asana ticket first in the engineering project (see Related Projects below).
- **Run the pre-session checklist** at `docs/pre-session-checklist.md` before writing any code.
- **Update this file** at the end of every session if architecture, tables, or edge functions changed.

## Known Issues

- Facebook snapshot URLs (`snapshot_url` in `competitor_ads`) may be blocked by Facebook's CSP when rendered in iframes, needs live verification. If blocked, consider server-side screenshotting as fallback.
- Local `src/` directory may be empty, code has been pushed directly to GitHub via MCP in previous sessions. Always check GitHub for the source of truth.
- Shares Supabase tables (static_*) that also appear in the creative-kitchen-video-v3 database.
- Foreplay API credits are limited (10,000 per period). Edge function has a `credit_budget` safeguard (default 500) and logs usage to `foreplay_credit_log`. Be careful with exploratory API calls.
- Foreplay Spyder only started tracking Simmer from ~Jan 11, 2026, no historical data before that date.
- Duplicate `CompetitorAds.jsx` exists in `src/` and `src/components/`. The `src/` copy is stale; Phase 3 cleanup.
- `debug-auth` is soft-retired (returns 410 Gone) as of 16 Apr. Hard-delete once caller logs confirm no traffic for 7 days, tracked in the Asana engineering project.

## Related Projects

- **creative-kitchen-video-v3** — sister project (video content), same Marketing department.
- **willa-hq** — loads this tool via iframe as "Static Assets" under Marketing.
- **Asana engineering project** — [project 1214024873723525](https://app.asana.com/1/5717506944667/project/1214024873723525/list/1214024873723542). Tracks stabilisation backlog, Video Analysis work, and Organic Intelligence milestones.

## Conventions

- Vite + React (not Next.js)
- Dark theme throughout
- Review ratings: great, good, needs-work, slop
- Reviewers: "claude" (AI) or "user" (human)
- Prompt versioning for iterative improvement
- Writing style: no em dashes or en dashes. Use commas, colons, full stops, or arrows (→). Write ranges as "2 to 3" not "2-3".
